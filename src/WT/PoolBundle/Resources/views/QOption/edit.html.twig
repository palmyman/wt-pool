{% extends 'WTPoolBundle::layout.html.twig' %}

{% block body -%}
    <h1>QOption edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('qoption_new', { 'questionId': entity.question.id}) }}">
            Back to the question options
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
